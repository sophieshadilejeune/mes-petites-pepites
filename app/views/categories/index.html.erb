<div class="container">
<div class="categories">
  <% @categories.each do |category| %>
  <%= link_to "#{category.name}", category_path(category), class: "btn btn-ghost btn-area-fill" %>
  <% end %>
</div>
</div>
